/** İlan detayında bölümler arası dikey boşluk (tek kaynak). */
export const LISTING_DETAIL_SECTION_GAP_Y = 'gap-y-10'

export const LISTING_DETAIL_SECTION_GAP = `flex flex-col ${LISTING_DETAIL_SECTION_GAP_Y}`

/** Yığılmış bölüm kartı — alt border/padding yok. */
export const LISTING_SECTION_SHELL = 'listingSection__wrap--stacked'

/** Anchor scroll + yığılmış kart */
export const LISTING_SECTION_STACKED = `${LISTING_SECTION_SHELL} scroll-mt-28`
